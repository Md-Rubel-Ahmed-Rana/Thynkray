import { Box } from "@mui/material";

type Props = {
  total: number;
  page: number;
  limit: number;
};

const Pagination = ({ total, limit, page }: Props) => {
  return <Box></Box>;
};

export default Pagination;
